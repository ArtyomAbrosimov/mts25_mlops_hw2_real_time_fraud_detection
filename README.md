# Real-Time Fraud Detection System

Система для обнаружения мошеннических транзакций в реальном времени с использованием ML-модели и Kafka для потоковой обработки данных.

## 🏗️ Архитектура

Компоненты системы:
1. **`interface`** (Streamlit UI):
   - Загрузка CSV-файлов с транзакциями
   - Отправка транзакций в Kafka с уникальными ID
   - Просмотр результатов: фродовые транзакции и распределение скоров
   - Две вкладки: "Отправка данных" и "Результаты"
    

2. **`fraud_detector`** (ML Service):
   - Чтение транзакций из топика Kafka `transactions`
   - Загрузка предобученной модели CatBoost (`catboost_model.cbm`)
   - Выполнение препроцессинга данных:
     - Извлечение временных признаков
     - Расчет гео-расстояний
     - Кодирование категориальных переменных
   - Скоринг с порогом 0.98
   - Отправка результатов в топик Kafka `scores`

3. **`result_saver`** (Database Service):
   - Чтение результатов из топика `scores`
   - Сохранение в PostgreSQL базу данных
   - Поддержка истории транзакций

4. **`PostgreSQL`**:
   - Хранение результатов скоринга
   - Таблица `transaction_scores` с полями: transaction_id, score, fraud_flag, created_at

5. **`Kafka Infrastructure`**:
   - Zookeeper + Kafka брокер
   - `kafka-setup`: автоматически создает топики `transactions` и `scores`
   - Kafka UI: веб-интерфейс для мониторинга сообщений (порт 8080)

## 🚀 Быстрый старт

### Требования
- Docker 20.10+
- Docker Compose 2.0+

### Запуск
```bash
docker-compose up --build
```
После запуска:
- **Streamlit UI**: http://localhost:8501
- **Kafka UI**: http://localhost:8080

## 🛠️ Использование

### 1. Загрузка данных:

 - Загрузите CSV через интерфейс Streamlit. Для тестирования работы проекта используется файл формата `test.csv` из соревнования https://www.kaggle.com/competitions/teta-ml-1-2025
 - Пример структуры данных:
    ```csv
    transaction_time,amount,lat,lon,merchant_lat,merchant_lon,gender,...
    2023-01-01 12:30:00,150.50,40.7128,-74.0060,40.7580,-73.9855,M,...
    ```
 - Для первых тестов рекомендуется загружать небольшой семпл данных (до 100 транзакций) за раз, чтобы исполнение кода не заняло много времени.

### 2. Мониторинг:
 - **Kafka UI**: Просматривайте сообщения в топиках transactions и scoring
 - **Логи обработки**: /app/logs/service.log внутри контейнера fraud_detector

### 3. Результаты:
- Перейдите на вкладку "📊 Результаты"
- Просмотрите последние фродовые транзакции (fraud_flag = 1)
- Изучите распределение скоров последних 100 транзакций
- Обновите данные по кнопкам "Обновить данные" и "Обновить график"
- Ознакомьтесь со статистикой базы данных

 - Скоринговые оценки пишутся в топик scoring в формате:
    ```json
    {
    "score": 0.995, 
    "fraud_flag": 1, 
    "transaction_id": "d6b0f7a0-8e1a-4a3c-9b2d-5c8f9d1e2f3a"
    }
    ```
## Структура проекта
```
.
├── fraud_detector/          # ML сервис скоринга
│   ├── app/                # Код запуска модели
│   │   ├── app.py
│   ├── src/                # Код модулей модели
│   │   ├── preprocessing.py
│   │   ├── scorer.py
│   ├── models/             # Предобученные модели
│   │   └── catboost_model.cbm
│   ├── train_data/         # Тренировочные данные
│   │   └── train.csv
│   ├── app/                # Логи работы
│   │   ├── app.py
│   ├── requirements.txt
│   └── Dockerfile
├── result_saver/           # Сервис сохранения в БД
│   ├── app.py
│   ├── requirements.txt
│   └── Dockerfile
├── interface/              # Streamlit UI
│   ├── app.py
│   ├── requirements.txt
│   ├── Dockerfile
│   └── .streamlit/
│       └── config.toml
└── docker-compose.yml
```

## Настройки Kafka
```yml
Топики:
- transactions (входные данные)
- scoring (результаты скоринга)

Репликация: 1 (для разработки)
Партиции: 3
```

*Примечание:* 

Для полной функциональности убедитесь, что:
1. Модель `catboost_model.cbm` размещена в `fraud_detector/models/`
2. Тренировочные данные находятся в `fraud_detector/train_data/`
3. Порты 8080, 8501 и 9095 свободны на хосте
